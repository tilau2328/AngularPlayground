const Artists = require('./artists'),
      Bands = require('./bands'),
      Albums = require('./albums'),
      Tracks = require('./tracks');

module.exports = {
    artists: Artists,
    bands: Bands,
    albums: Albums,
    tracks: Tracks
};